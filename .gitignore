# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Node / Cloudflare
node_modules/
dist/
.dev.vars
.wrangler/
*.local
.env

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# wrangler local test state
/.wrangler-test-state/
worker/.wrangler-test-state/

# AI agent wiring ที่เป็นรายเครื่อง (commit เฉพาะ .claude/ ที่แชร์ทั้งทีม)
/.codex/
/.cursor/
/.gemini/
/.kiro/
/.windsurf/
/.scratch/
/.ignore
Code/.agent/
Code/.agents/
Code/.codex-backups/
Code/.cursor/
Code/.gemini/
Code/.kiro/
Code/.windsurf/
Code/.scratch/
Code/.ignore

# local-only: ไม่ใช่ไฟล์เว็บ (logs, ภาพ verify, โน้ตส่วนตัว)
.grok/
.playwright-cli/
/verification_dashboard.png
/*Lampangpround/

# prod data dumps (มีข้อมูลผู้ใช้จริง ห้าม commit)
/backups/

# local wrangler state ที่เก็บชื่ออื่นช่วง migration
worker/.wrangler-four-fixes/
